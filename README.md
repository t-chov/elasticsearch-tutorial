# elasticsearch-tutorial

Elasticsearch tutorial.

## how to use

1. Use docker-compose
2. Provision data
    - It uses IMDb datasets.
    - Data will be in `rawdata/`

```sh
$ docker-compose up
$ sh provision.sh
```